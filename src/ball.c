#include "ball.h"
Ball ball_add(Ball lo, Ball ro) {
    Ball ans;
    ans.radius = 0;
    return ans;
}
Ball ball_multiply(Ball lo, Ball ro) {
    Ball ans;
    ans.radius = 0;
    return ans;
}
Ball ball_div(Ball lo, Ball ro) {
    Ball ans;
    ans.radius = 0;
    return ans;
}
